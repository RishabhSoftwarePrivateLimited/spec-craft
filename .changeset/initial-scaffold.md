---
"@rspl/speccraft": minor
---

Add the speccraft CLI: scaffolds the agent-agnostic `spec/` stage-gated SDD workflow plus optional Claude, Gemini, Copilot, and agent-agnostic command layers into a project, with docs, worked examples, and open-source repo setup.
