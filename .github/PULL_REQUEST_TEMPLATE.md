## What does this change?

## Why?

## Checklist

- [ ] `npm test` passes locally
- [ ] Added/updated a test in `test/` for any `src/` or `templates/` change
- [ ] Added a changeset (`npx changeset`) describing the change and version bump
- [ ] Updated `docs/*.md` if user-facing behavior changed
