# Screenshots

Referenced from `docs/setup.md` — one terminal capture (PNG) per prompt step (target dir, agent multi-select, git-hook confirm, final confirm), plus the multi-select mid-selection and the resulting `tree` output for both the claude-only and all-agents runs.

**Not yet captured.** These require a real interactive terminal session, which isn't something that can be produced from a non-interactive tool run — capture them by hand (or with a terminal-recording tool) before the first release, then drop the PNGs in this folder using the names `docs/setup.md` links to.

In the meantime, `examples/claude-only/TREE.txt` and `examples/all-agents/TREE.txt` are real (non-screenshot) renders of the resulting file trees.
